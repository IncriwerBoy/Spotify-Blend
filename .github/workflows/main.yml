name: CI/CD

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v2

      - name: Set up Python
        uses: actions/setup-python@v2
        with:
          python-version: 3.7

      - name: Install Dependencies
        run: |
          pip install -r requirements.txt

      - name: Run Tests
        run: |
          python -m pytest

          - name: Use CLIENT_ID from GitHub Secrets
          run: |
            echo ${{ secrets.CLIENT_ID }}  # Access the secret
  

            - name: Use CLIENT_ID from GitHub Secrets
            run: |
              echo ${{ secrets.CLIENT_ID }}  # Access the secret
    
        run: |
          deploy_script.sh
